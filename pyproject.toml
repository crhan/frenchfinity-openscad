[project]
name = "frenchfinity-openscad-tools"
version = "0.1.0"
description = "Python tooling for reverse engineering and validating Frenchfinity OpenSCAD parts."
requires-python = ">=3.12"
dependencies = [
    "build123d>=0.11,<0.12",
    "numpy>=2,<3",
    "scipy>=1.14,<2",
]

[tool.uv]
package = false
